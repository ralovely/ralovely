{
  "name": "ralovely",
  "version": "1.0.0",
  "private": true,
  "description": "Personal blog and static website",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "shiki": "^1.0.0"
  }
}
